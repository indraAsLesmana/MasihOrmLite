<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tutor93.tugasfrensky">

    <!--permision camera-->
    <uses-permission android:name="android.permission.CAMERA" />
    <!--permission external storage-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!--permission internet-->
    <uses-permission android:name="android.permission.INTERNET" />
    <!--permission phone call-->
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".activity.MainActivity" />

        <activity android:name=".activity.Add_employee">

        </activity>
        <activity android:name=".fragment.LatihanApiMain_fragment" />
        <activity android:name=".activity.ApiEmployeeForm" />
        <activity android:name=".activity.MainMenu">

        </activity>

        <activity
            android:name=".activity.MainFragment"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>


    </application>

</manifest>